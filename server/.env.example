PORT=5050
JWT_SECRET=change-this-to-a-long-random-string

# Optional: real SMTP for sending invoice emails.
# Leave empty to use a free Ethereal test inbox (a preview link is shown after sending).
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
